name: 'My Composite Action'
description: 'Configures AWS credentials and performs additional steps'
inputs:
  aws-access-key-id:
    description: 'AWS Access Key ID'
    required: true
  aws-secret-access-key:
    description: 'AWS Secret Access Key'
    required: true
  aws-region:
    description: 'AWS Region'
    required: true
  # Add other inputs as needed

runs:
  using: "composite"
  steps:
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ inputs.aws-access-key-id }}
        aws-secret-access-key: ${{ inputs.aws-secret-access-key }}
        aws-region: ${{ inputs.aws-region }}


# name: 'Composite Setup'
# description: 'Checkout and configure AWS credentials'

# inputs:
#   aws-region:
#     description: 'AWS region for configuration'
#     required: true

# runs:
#   using: 'composite'
#   steps:
#     # - name: Checkout Repo
#     #   uses: actions/checkout@v2
#     - name: config aws
#       uses: aws-actions/configure-aws-credentials@v1
#       with:
#         aws-access-key-id: ${{ secrets.GHUB_ACTIONS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.GHUB_ACTIONS_ACCESS_KEY }}
#         aws-region: ${{ inputs.aws-region }}
